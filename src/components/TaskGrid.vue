<template>
    <div class="task-grid">
        <template v-if="tasks.length">
            <Task v-for="task in tasks" :key="task.name" :task="task" @taskDeleted="$emit('taskDeleted', $event)" @taskStateChanged="$emit('taskStateChanged', $event)"></Task>
        </template>
        <p v-else class="no-task">Sua vida está em dia :)</p>
    </div>
</template>

<script>
import Task from '@/components/Task.vue'

export default {
    components: { Task },
    props: {
        tasks: {type: Array, required: true}
    }
}
</script>

<style scoped>
    .task-grid {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;

    }    

    .no-task {
        color: #AAA;
        font-size: 1.7rem;
    }
</style>